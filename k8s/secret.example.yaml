# Example only — in production use Sealed Secrets / External Secrets / cloud KMS.
apiVersion: v1
kind: Secret
metadata:
  name: annotation-secrets
  namespace: annotation
type: Opaque
stringData:
  SECRET_KEY: "REPLACE_WITH_32B_HEX"
  POSTGRES_USER: "annotator"
  POSTGRES_PASSWORD: "REPLACE_ME"
  S3_ACCESS_KEY: "REPLACE_ME"
  S3_SECRET_KEY: "REPLACE_ME"
  OPENAI_API_KEY: ""
